<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net9.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UseWPF>true</UseWPF>
    <UseWindowsForms>true</UseWindowsForms>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <AssemblyName>ScreenPaste</AssemblyName>
    <RootNamespace>ScreenPaste</RootNamespace>
    <LangVersion>latest</LangVersion>
    <ApplicationIcon>Assets\app.ico</ApplicationIcon>
  </PropertyGroup>

  <!-- Product / version metadata (overridable at build via -p:Version=x.y.z) -->
  <PropertyGroup>
    <Product>ScreenPaste</Product>
    <Company>ScreenPaste</Company>
    <Authors>taida957789</Authors>
    <Description>截圖與標註工具</Description>
    <Copyright>Copyright © 2026</Copyright>
    <Version>1.7.1</Version>
    <AssemblyVersion>1.7.1.0</AssemblyVersion>
    <FileVersion>1.7.1.0</FileVersion>
  </PropertyGroup>

  <ItemGroup>
    <!-- Decodes PNG / JPEG / WebP for the paste-image (sticker) feature. -->
    <PackageReference Include="SkiaSharp" Version="2.88.8" />
  </ItemGroup>

  <!-- Bundle ffmpeg for region recording. Place the binary at ffmpeg\ffmpeg.exe
       (scripts/fetch-ffmpeg.ps1); the wildcard is a no-op until it exists so the
       project still builds without it. ExcludeFromSingleFile keeps it a loose file
       next to the exe on publish — the installer and portable zip package it from
       there, and FFmpegLocator finds it via AppContext.BaseDirectory. -->
  <ItemGroup>
    <None Include="ffmpeg\**\*" CopyToOutputDirectory="PreserveNewest" LinkBase="ffmpeg\"
          ExcludeFromSingleFile="true" />
  </ItemGroup>

  <!-- Keep WinForms/Drawing OUT of the implicit global usings so WPF types
       (Color, Point, Button, MouseEventArgs, …) resolve unambiguously.
       The two files that need GDI+ import System.Drawing explicitly. -->
  <ItemGroup>
    <Using Remove="System.Windows.Forms" />
    <Using Remove="System.Drawing" />
  </ItemGroup>

</Project>
